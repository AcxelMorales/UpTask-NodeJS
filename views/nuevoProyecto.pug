extends layout

include mixins/crearProyecto

block contenido
  .contenido-principal
    h1= nombrePagina

    if errores
      each e in errores
        .alerta.error= e.texto

    +crearProyecto(proyecto)
